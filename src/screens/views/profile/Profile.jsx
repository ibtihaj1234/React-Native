import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ProfComp from '../../../component/profileComp/ProfComp'

const Profile = () => {
  return (
    <View style={{flex:1}}>
      <ProfComp />
    </View>
  )
}

export default Profile

const styles = StyleSheet.create({})