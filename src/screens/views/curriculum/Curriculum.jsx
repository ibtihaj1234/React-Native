import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import CurriculumComp from '../../../component/curriculumComp/CurriculumComp'

const Curriculum = () => {
    return (
        <View style={{ flex: 1 }}>
            <CurriculumComp />
        </View>
    )
}

export default Curriculum

const styles = StyleSheet.create({})